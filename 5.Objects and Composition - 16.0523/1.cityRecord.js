function cityRecord(name, population, treasury) {
    let cityInfo = {
        name: name,
        population: population,
        treasury: treasury
    }

    return cityInfo
}

cityRecord('Tortuga',

    7000,

    15000)