function addAndRemoveEl(arr) {
    let result = 0;

    for (let i = 0; i < arr.length; i++) {
        if (arr[i] === 'add') {
            result++;
            console.log(result);
        } else if (arr[i] ===) {
            result++
        }

    }

}

addAndRemoveEl(['add',
    'add',
    'remove',
    'add',
    'add']
)